export enum ServerRoutes {
  words = "/words",
  users = "/users",
  signin = "/signin",
  statistics = "/statistics",
  settings = "/settings",
  tokens = "/tokens",
  aggregatedWords = "/aggregatedWords",
}
